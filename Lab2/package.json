{
  "name": "currency-converter-mcp",
  "version": "0.1.0",
  "description": "A currency converter MCP server using the official MCP TypeScript SDK on Azure Functions.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "func start",
    "test": "vitest run"
  },
  "dependencies": {
    "@azure/functions": "^4.9.0",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=22"
  }
}
